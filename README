#Criar o Ambiente Virtual
-- python3 -m venv venv

# Ativar o Ambiente Virtual
-- source venv/bin/activate

# Rodar antes de qualquer operação, limpeza de ambiente da mininet
-- sudo mn -c

# Roda no Terminal 1
-- osken-manager --ofp-tcp-listen-port 6633 osken_controller.py

# Roda no terminal 2
-- sudo python3 run_teste.py




sudo mn -c
sudo killall -9 osken-manager iperf3

